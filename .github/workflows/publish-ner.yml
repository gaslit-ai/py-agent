name: publish py-agent-ner

# Triggered by tags of the form `ner-vX.Y.Z`. Builds and uploads
# `packages/py-agent-ner` to PyPI via trusted publishing (OIDC).
#
# Setup once per PyPI project:
#   PyPI → Manage → Publishing → Add a new pending publisher
#     Owner:    gaslit-ai
#     Repo:     py-agent
#     Workflow: publish-ner.yml
#     Env:      pypi
on:
  push:
    tags: ["ner-v*"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Verify tag matches package version
        working-directory: packages/py-agent-ner
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#ner-v}"
          pkg=$(uv run --with hatch hatch version)
          if [ "$tag" != "$pkg" ]; then
            echo "Tag version ($tag) does not match package version ($pkg)" >&2
            exit 1
          fi

      - name: Build sdist and wheel
        run: uv build --package py-agent-ner --sdist --wheel

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: py-agent-ner-dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/py-agent-ner/
    permissions:
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: py-agent-ner-dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
